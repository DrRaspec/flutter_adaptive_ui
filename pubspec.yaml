name: flutter_adaptive_kit
description: A comprehensive Flutter package for building responsive and adaptive UIs. Combines design size scaling, device detection, breakpoints, and extension methods in one unified API.
version: 0.1.0
homepage: https://github.com/DrRaspec/flutter_adaptive_ui
repository: https://github.com/DrRaspec/flutter_adaptive_ui
issue_tracker: https://github.com/DrRaspec/flutter_adaptive_ui/issues

topics:
  - responsive
  - adaptive
  - ui
  - screen-size
  - breakpoints

environment:
  sdk: ^3.10.4
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
